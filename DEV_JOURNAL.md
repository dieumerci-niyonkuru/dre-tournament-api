# Dev Journal

- Git init in wrong folder - fixed by reinitializing
- Forgot unique_together - added in second migration
- Deadline used date.today() - fixed with timezone.now()
- Tests missing /api/ prefix - corrected
- cat commands interrupted - used echo instead
